# This repo contains examples of c++ module usage

## Folder compiler only
Obviously contains compiler-only example (without build system CMake, Ninja, etc...)

## Cmake example

## compile std modules by yourself

## import std